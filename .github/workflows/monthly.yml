name: Monthly Analytics Update

permissions:
  contents: write

on:
  schedule:
    - cron: '0 6 10 * *'
  workflow_dispatch:

jobs:
  build-analytics:
    runs-on: ubuntu-latest

    env:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}

    steps:
      # =========================
      # CHECKOUT
      # =========================
      - name: Checkout repository
        uses: actions/checkout@v4

      # =========================
      # PYTHON SETUP
      # =========================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # =========================
      # INSTALL DEPENDENCIES
      # =========================
      - name: Install dependencies
        run: pip install -r requirements.txt

      # =========================
      # RUN SCRIPT (JSON)
      # =========================
      - name: Run aggregation pipeline
        run: python src/build_aggregations.py

      # =========================
      # NEW: CONVERT JSON → CSV
      # =========================
      - name: Convert JSON to CSV
        run: python src/build_csv.py

      # =========================
      # DEBUG
      # =========================
      - name: List generated files
        run: |
          echo "Files in data folder:"
          ls -la data || echo "No data folder"

          echo "Files in data_csv folder:"
          ls -la data_csv || echo "No CSV folder"

      # =========================
      # COMMIT & PUSH
      # =========================
      - name: Commit and push JSON + CSV files
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add data/*.json
          git add data_csv/*.csv

          git diff --cached --quiet || git commit -m "auto-update trade data (JSON + CSV)"

          git push
